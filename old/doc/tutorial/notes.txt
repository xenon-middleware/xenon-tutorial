

setting up PMD in eclipse local install

passwordless login to localhost via ssh

Eclipse default compiler compliance level
    window -> preferences -> Java -> compiler then set Compiler compliance level to 1.7 or higher

building section largely obfuscated
